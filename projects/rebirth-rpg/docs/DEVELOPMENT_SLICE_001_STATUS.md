# Rebirth RPG — Development Slice 001 Status

> updated: 2026-09-08
> status: CODE WRITTEN / WAITING FOR STUDIO SCRIPT SYNC + CORE SMOKE TEST

This file records actual implementation progress. File count is not treated as gameplay completion.

## CODE WRITTEN

### Shared protocol
- narrow Remote names
- enemy tag/attribute contract
- stable project IDs independent from visual asset names
- attack state hook for future animation/VFX presentation

### Boot / validation
- staged server boot logs so first failing subsystem is easier to identify
- runtime `ConfigValidator` checks core catalog/protocol consistency before services start
- duplicate Remote names rejected
- missing loot/item/enemy references rejected
- malformed first-slice combat/rebirth config rejected early
- optional Studio smoke harness is isolated with `pcall` so debug-harness failure cannot become a production boot single point of failure

### Server profile / progression
- test-safe in-memory profile
- Level / XP / Gold
- Rebirth count
- milestone flags
- region/boss flags
- inventory ownership
- equipped mainhand
- server-owned equip validation

### Rebirth
- server validates Slice test level requirement + boss clear flag
- resets Level / XP / Gold / current region progress
- retains owned/equipped items provisionally until equipment reset policy is playtested
- automatic XP / Gold / Luck / StartPower multipliers
- milestone flags at R1/R2/R3/R5/R7/R10
- Studio-only test hooks; no production cheat Remote

### Combat
- server-owned AttackIntent / SkillIntent
- client cannot submit damage, target list, combo index or reward amount
- cooldown + busy-state validation
- three-hit basic combo generated by server state
- one simple weapon skill per current abstract weapon style
- server `GetPartBoundsInBox` melee query
- target dedup per attack
- line-of-sight validation
- Level/Rebirth damage multiplier
- kill-claim guard against duplicate reward

### Enemy runtime
- activation only through project tag + `EnemyId`
- project sets health / move speed / aggro / windup / attack / reward / respawn
- bundled third-party AI is not used
- **tagged enemy models containing Script/LocalScript/ModuleScript are refused by EnemyService**
- simple chase/attack loop
- target distance rechecked after windup
- server network ownership attempted for unanchored enemy parts
- respawn uses an Archivable-safe sanitized rig clone

### Rewards / equipment
- XP and Gold awarded only by server
- loot rolls only by server
- Enemy B deterministically grants the first meaningful `weapon_start_b` upgrade
- unique weapon drop dedupe prevents repeated stacking
- inventory ownership is separate from any future Tool/Model visual
- equip request is ownership/catalog validated and rate-limited

### Client shell
- internal HUD for Level / XP / Gold / Rebirth / objective
- internal Weapon B equip button
- desktop/gamepad/touch attack
- desktop/gamepad/touch skill
- Rebirth test input
- server-driven hit/reward feedback text shell

### Studio-only smoke harness
- guarded by `RunService:IsStudio()`
- opt-in Workspace attribute `RebirthRPG_EnableSmokeHarness`
- requires explicit named anchor `RebirthRPG_SmokeOrigin`
- creates sanitized temporary R15 debug rigs for Enemy A / Enemy B / Boss
- uses anchor-relative placement, not guessed world coordinates
- never production art

### Integration
- Script Sync DataModel layout defined in `SCRIPT_SYNC_RUNTIME_LAYOUT_001.md`
- Studio smoke route defined in `STUDIO_CORE_SMOKE_TEST_001.md`
- user handoff remains `.rbxlx` after actual Studio validation

## CURRENT FIRST-SLICE ABSTRACT CONTENT

Stable gameplay IDs, **not final visual names**:

```text
weapon_start_a
weapon_start_b

enemy_field_a_01
enemy_field_b_01
boss_region_01
```

Actual display identities and visual mappings remain blocked on Studio asset approval.

## NOT YET FEATURE-IMPLEMENTED

The current source cannot yet be called an implemented playable feature because it has not passed Studio Play.

Missing/blocked:
- successful Script Sync into exact target place
- clean Studio boot / Output evidence
- approved environment assets
- approved weapon visuals/grips
- approved enemy rigs/animations
- attack animations
- hit VFX/audio/camera response
- enemy telegraph presentation
- boss AoE presentation/logic
- dash/movement polish
- production inventory UI
- quests
- secrets/chests
- first-zone map
- persistence across server sessions

## KNOWN TECHNICAL LIMITATIONS

- Profile state is intentionally in-memory for the first slice; DataStore persistence is not claimed.
- Enemy pursuit uses direct `Humanoid:MoveTo`, not obstacle-aware path planning. Adopt Pathfinding only if the actual combat space requires it.
- Current HUD is an internal test shell, not production UI.
- Numeric tuning is provisional until Studio TTK measurements exist.
- Attack/skill presentation hooks have no approved animation/VFX attached.
- Full Roblox AuthorityMode adoption is not assumed; valuable combat/progression decisions are nevertheless server-authoritative in project code.
- The Studio smoke harness itself has not yet been executed in Studio.

## TESTED

Completed outside Studio:
- latest GitHub source/docs inspected
- current project source manually reviewed against Godbase server-authority, hitbox, inventory, failure-library, Script Sync and rebirth contracts
- current Roblox Script Sync documentation rechecked
- obvious architectural risks fixed: optional smoke harness boot isolation, unsanitized enemy executable rejection, config/protocol Gate 0 validation

Not yet tested:
- Roblox Studio Script Analysis
- Studio Play clean boot
- Output error count
- actual hitbox volume/timing
- debug rig movement/attack behavior
- death/respawn runtime route
- rebirth runtime route
- mobile device simulation
- production P0 route

## NEXT SAFE EXECUTION

Do not add another region or large system layer now.

```text
inspect clean Rebirth RPG Studio place
→ Script Sync current code
→ enable Studio-only smoke harness from explicit anchor
→ clean boot
→ attack → death → reward
→ Enemy B → Weapon B → equip
→ boss clear
→ death/respawn
→ rebirth test
→ report / smallest coherent fixes
→ exact failed-route replay
→ only then asset TASK 01–04
→ asset approval
→ first-zone spatial plan
```

The next quality gate is **actual Studio evidence**, not more code volume.
